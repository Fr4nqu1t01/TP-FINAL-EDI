let x = 5;
let y = 12;

function suma(x, y) {
  return x + y;
}
// lo que esta entre parentesis (), EJECUTAN la accion
let respuesta = suma(x, y);
console.log(`la respuesta de ${x} + ${y} es igual a:`, respuesta);